#include "main.h"
#include <stdio.h>

/**
 * _isdigit - the digit
 *@c: parameter for digit
 *Return: it is 0
 */

int _isdigit(int c)
{
	if (_isdigit(c))
		printf("%c: 1", c);
	else
		printf("%c: 0", c);
	return (0);
}
