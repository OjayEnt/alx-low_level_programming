#include "main.h"
#include <stdio.h>
#include <ctype.h>

/**
 * _isupper - upper case
 * @c: the alphabet
 * Return: it is 0
 */

int _isupper(int c)
{
	if (_isupper(c))
		printf("%c: 1\n", c);
	else
		printf("%c: 0\n", c);
	return (0);
}
