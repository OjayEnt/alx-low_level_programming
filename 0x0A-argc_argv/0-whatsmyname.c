#include <stdio.h>

/**
 * main - the argument
 * @argc: argument count
 * @argv: argument vector
 * Return: it returns to zero
 */

int main(int __attribute__((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
